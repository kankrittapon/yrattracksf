# บันทึกการเปลี่ยนแปลง

เอกสารนี้สรุปการเปลี่ยนแปลงสำคัญของระบบ SailFish Race Intelligence

## 2026-07-29

### การแข่งขันสด

- เพิ่มหน้าติดตามการแข่งขันสดสำหรับรอบที่ SailFish มีสถานะ `50` หรือกำลังแข่งขัน
- รอบสถานะ `10` ที่ Admin กดเตรียมเก็บข้อมูลแล้วจะแสดงในหน้าการแข่งขันสด
  พร้อมสถานะ Collector, WebSocket และข้อความว่ากำลังรอ Start
- แสดงแผนที่การแข่งขัน ตำแหน่งนักกีฬา และทุ่นลมสำหรับสมาชิกที่เข้าสู่ระบบ
- เพิ่มตารางลมพร้อมตัวเลือกเวลาจริง, 3 นาที, 5 นาที และ 10 นาที
- เพิ่มตารางนักกีฬาที่แสดง SOG, COG และค่าทิศทุ่นลม − COG
- หน้า Public สามารถเลือกนักกีฬาเพื่อดู SOG, COG, ความเร็วลม, ทิศลม,
  มุมเส้นทางเทียบลม และกราฟตามเวลา
- หน้า Dashboard รับการเปลี่ยนแปลงจาก Supabase Realtime และโหลดข้อมูลใหม่เมื่อ
  นักกีฬา ลม หรือสถานะ Collector เปลี่ยน
- บันทึก Audit Log เมื่อสมาชิกเปิดดูหน้าการแข่งขันสด

### ระบบ Track และ GPS ก่อนเริ่มการแข่งขัน

- เพิ่มตัวตรวจ Snapshot ก่อนเริ่มการแข่งขันทุกประมาณ 5 วินาที
- ตรวจสถานะ Track/GPS แยกตามรอบการแข่งขันและนักกีฬา
- รองรับกรณีเลข Track ของนักกีฬาคนเดิมเปลี่ยนไปในแต่ละการแข่งขัน
- หน้า Public แสดงเฉพาะสถานะภาพรวมว่าเปิดระบบติดตามแล้ว
- จำกัดเลข Track จำนวน GPS ออนไลน์ และสถานะรายเครื่องให้ Admin เท่านั้น
- ไม่เปิดเผยพิกัดก่อนการแข่งขันผ่าน Public API

### ข้อมูลย้อนหลัง

- เพิ่มการเลือกรายการแข่งขัน ประเภทเรือ รอบ และนักกีฬา
- เพิ่มการนำเข้าข้อมูลย้อนหลังจาก SailFish หลังจบรอบ
- แก้การนำเข้า Athlete batch ให้รองรับข้อมูลที่มี schema ต่างกัน
- เพิ่มข้อมูลลมย้อนหลังและทิศลมล่าสุดของรอบ
- เพิ่ม SOG, COG, มุมเทียบลม และกราฟความเร็วรายนักกีฬา
- Public History จะแสดงเฉพาะรอบสถานะ `99` ที่นำเข้าสำเร็จและ Admin อนุญาต

### หน้าควบคุมและการเผยแพร่

- ปรับลำดับการทำงานเป็น รายการแข่งขัน → ประเภทเรือ → รอบการแข่งขัน
- เพิ่มการค้นหารายการจาก SailFish และ Sync รอบใหม่
- เพิ่มคำสั่งเตรียมเก็บข้อมูล, เริ่มด้วยตนเอง, หยุด และลองใหม่
- เพิ่มการเปิดเผย Public ระดับรายการและประเภทเรือ
- แยกสิทธิ์เผยแพร่การแข่งขันสดออกจากข้อมูลย้อนหลัง
- เพิ่มสถานะการเชื่อมต่อ `ai-brain` ผ่าน Tailscale

### Backend และฐานข้อมูล

- เพิ่ม FastAPI Collector พร้อมสถานะ
  `idle → armed → waiting_for_start → recording → finishing → completed/error`
- เพิ่มการตรวจสถานะ SailFish `10` รอเริ่ม, `50` กำลังแข่งขัน และ `99` จบแล้ว
- เพิ่มการ reconnect WebSocket และ subscribe หัวข้อ Athlete, Wind และ Race Control
- เพิ่มตาราง normalized readings, latest state, raw messages, data quality และ audit logs
- เพิ่ม History Import scheduler และระบบ retry
- เปิด RLS และแยกสิทธิ์ `viewer`, `admin`, Public และ Backend service role
- จำกัดข้อมูลควบคุม Backend ผ่าน Tailscale เท่านั้น

### การแสดงผลและภาษา

- แปลเมนูและข้อความหลักเป็นภาษาไทย
- ปรับหน้าควบคุมและ Public ให้รองรับโทรศัพท์มือถือ
- เพิ่มคำอธิบายสถานะว่างและปุ่มนำผู้ดูแลไปยังหน้าควบคุม

### ข้อควรทราบก่อนทดสอบการแข่งขันสด

- ต้องตั้งค่า `SAILFISH_LIVE_TOKEN` บน `ai-brain`
- Admin ต้องกด **เตรียมเก็บข้อมูล** ให้รอบปัจจุบันก่อนเจ้าของสนามกด Start
- หน้า **การแข่งขันสด** แสดงรอบสถานะ `10` เมื่อกดเตรียมเก็บข้อมูลแล้ว
  และเปลี่ยนเป็นหน้าข้อมูลเต็มเมื่อสถานะเปลี่ยนเป็น `50`
- ข้อมูลสดจะสมบูรณ์เมื่อ Collector อยู่สถานะ `recording` และ WebSocket เชื่อมต่อ

## 2026-07-28

### เริ่มต้นระบบ

- สร้างโครง Next.js + TypeScript สำหรับ Vercel
- สร้าง FastAPI Backend สำหรับ `ai-brain`
- เชื่อม Supabase PostgreSQL, Auth และ Realtime
- เพิ่ม Docker Compose, health check และการตั้งค่า Tailscale Serve
- เพิ่มโครงสร้างฐานข้อมูลและนโยบายความปลอดภัยเริ่มต้น
