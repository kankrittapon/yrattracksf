# สรุปงาน: ปรับดีไซน์หน้าตาให้เหมาะกับสมาคมเรือใบแห่งประเทศไทย (YRAT)

**ขอบเขต:** แก้เฉพาะ frontend (UX/UI, theme, styling, ฟอนต์, ข้อความแบรนด์บางจุด) — ไม่แตะ backend/logic และ **ยังไม่ได้ commit ใดๆ**

## แนวคิดการออกแบบ

เปลี่ยนธีมเดิมที่เป็นโทน "มินต์นีออน + เทคสตาร์ทอัพ" ให้เป็นโทน **น้ำเงินเข้ม (navy) + ทอง/ทองเหลือง (brass gold)** ให้ความรู้สึกเป็นสมาคมกีฬาเรือใบที่เป็นทางการ ดูพรีเมียม สะอาดตา และทันสมัย พร้อมยืมสัญลักษณ์การเดินเรือมาใช้เชิงความหมาย:

- **ทอง (gold)** = สีแบรนด์หลัก ใช้กับปุ่ม, สถานะ active, ตัวเลขข้อมูลเด่น, โลโก้
- **เขียว (starboard green)** = ใหม่ เพิ่มเข้ามาโดยเฉพาะสำหรับสถานะ "live / ปกติ / สำเร็จ / เปิดใช้งาน" (อ้างอิงไฟสัญญาณกราบขวาเรือ - นานาชาติเขียว=ไป) แยกออกจากสีทองของแบรนด์ เพื่อให้ความหมายชัดเจนขึ้นกว่าดีไซน์เดิมที่ใช้สีเดียวปนกันทั้งหมด
- **แดง (signal red)** = สถานะ error/offline/negative (คงไว้ตามเดิมแต่ปรับโทนให้เข้ากับธีมใหม่)
- **อำพัน (amber)** = ข้อมูลลม/ทิศทาง และคำเตือนทั่วไป

## สิ่งที่แก้ไข

1. **`frontend/src/app/globals.css`** — ปรับสีทั้งไฟล์ (มีการรีแมปสี hex กว่า 150 จุดให้เป็นระบบเดียวกัน), เพิ่มตัวแปรสี `--live` (เขียว) แยกจาก `--mint` (ทอง) และแก้ selector สถานะ (live/online/positive/enabled/all-clear/arm ฯลฯ) ให้ใช้สีเขียวแทนสีทองอย่างถูกความหมาย, ลดความมนขอบ (border-radius) ของการ์ด/โลโก้ให้ดูคมเรียบร้อยขึ้น, เปลี่ยนโลโก้ (brand mark) เป็นไล่สีทองแบบเหรียญตรา (medallion gradient)
2. **`frontend/src/app/layout.tsx`** — เพิ่มฟอนต์ **Prompt** (รองรับไทย+อังกฤษ, ทางการ, นิยมใช้ในเว็บหน่วยงาน/กีฬาไทย) ผ่าน `next/font/google`, แก้ title/description ให้กล่าวถึงสมาคมเรือใบแห่งประเทศไทย
3. **`frontend/src/app/icon.svg`** (ใหม่) — favicon สมอเรือทรงเรียบสีทองบนพื้นน้ำเงินเข้ม
4. **ข้อความแบรนด์** ในหน้า sidebar, login, public — เพิ่มชื่อ "สมาคมเรือใบแห่งประเทศไทย" กำกับใต้ชื่อระบบ SailFish (`dashboard-shell.tsx`, `login/page.tsx`, `public/page.tsx`)
5. **`.claude/launch.json`** (ใหม่) — ตั้งค่าไว้สำหรับรัน dev server ผ่าน preview tool ระหว่างตรวจสอบงาน (ไฟล์ config ของเครื่องมือ ไม่ใช่โค้ดแอป)

## การตรวจสอบ

- `npm run typecheck` ผ่าน
- รัน dev server ตรวจสอบจริงในเบราว์เซอร์: ยืนยันว่าฟอนต์ Prompt โหลดสำเร็จ, สีพื้นหลัง/ปุ่ม/ตัวแปร CSS ใหม่ทำงานถูกต้อง, และสถานะ "live" ใช้สีเขียวใหม่ (`#34a06c`) แยกจากสีทองแบรนด์ตามที่ตั้งใจ
- หน้า **login** เรนเดอร์และแสดงข้อความแบรนด์ใหม่ถูกต้อง
- หน้า **/overview** และ **/public** ไม่สามารถตรวจสอบภาพจริงได้ เนื่องจาก `.env.local` ปัจจุบันขาดค่า `NEXT_PUBLIC_SUPABASE_URL` (มีแต่ `NEXT_PUBLIC_SUPABASE_ANON_KEY`) ทำให้ Supabase client สร้างไม่สำเร็จ — **นี่เป็นปัญหาที่มีอยู่ก่อนแล้ว ไม่เกี่ยวกับการแก้ดีไซน์ครั้งนี้** แนะนำให้เติมค่า URL ใน `.env.local` แล้วลองดูหน้าที่เหลือ (live, history, compare, control, quality) อีกครั้ง เพราะทุกหน้าใช้ class เดียวกันจาก `globals.css` จึงควรได้รับธีมใหม่ทันทีโดยไม่ต้องแก้อะไรเพิ่ม
- เครื่องมือ screenshot ของ Browser pane ใช้งานไม่ได้ในสภาพแวดล้อมนี้ (พาเนลไม่แสดงผล) จึงตรวจสอบผ่าน computed styles / accessibility tree แทนการดูภาพหน้าจอโดยตรง

## ยังไม่ได้ทำ (นอกขอบเขตหรือรอการตัดสินใจ)

- ไม่ได้เปลี่ยนชื่อระบบจาก "SailFish" เป็นชื่ออื่น (คงไว้เป็นชื่อผลิตภัณฑ์ตามเดิม เพิ่มแค่ข้อความกำกับสมาคม)
- ไม่ได้แตะ favicon/emblem แบบมีตราสัญลักษณ์จริงของสมาคม (ถ้ามีไฟล์โลโก้ทางการ สามารถนำมาแทน `icon.svg` และ brand-mark ได้ภายหลัง)
- ยังไม่ได้ commit — รอให้ตรวจสอบหน้าตาจริงก่อน แล้วสั่ง commit ได้เมื่อพร้อม
